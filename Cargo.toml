[workspace]
members = ["pg-core", "pg-cli", "pg-pkg", "pg-ffi"]
exclude = ["pg-wasm"]

resolver = "2"

[profile.release]
lto = true

[profile.edge]
inherits = "release"
lto = false

[profile.release.package.pg-wasm]
opt-level = "s"
